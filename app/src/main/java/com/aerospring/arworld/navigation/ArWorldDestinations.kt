package com.aerospring.arworld.navigation

/** Единый список маршрутов приложения — чтобы строки не расползались по коду. */
object ArWorldDestinations {
    const val HOME = "home"
    const val WHERE_ANYTHING_IS = "where_anything_is"
}